name: september-17-preview
on:
  push:
    branches: [candidate/handedness-v1]
    paths: [.github/workflows/sep17-preview.yml]
permissions:
  contents: write
jobs:
  board:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -e '.[dev]'
      - run: python -m pytest -q
      - name: Update prior-day history
        run: daily-hr-ingest 2026-06-01 2026-09-16 data/raw/sep17_history.csv --chunk-days 7
      - name: Retrain corrected model
        run: |
          daily-hr-build data/raw/sep17_history.csv data/processed/sep17_training.parquet
          daily-hr-train data/processed/sep17_training.parquet --model-out models/sep17_pregame.joblib
      - name: Build preliminary boards
        run: |
          python -m daily_hr.daily_board data/raw/sep17_history.csv models/sep17_pregame.joblib models/sep17_pregame.features.json reports/sep17_inputs.csv --date 2026-09-17 --allow-projected
          python -m daily_hr.production_scorer reports/sep17_inputs.csv reports/sep17_v1_board.csv
          python - <<'PY'
          import pandas as pd
          for version in ('v1',):
              board = pd.read_csv(f'reports/sep17_{version}_board.csv')
              assert len(board) >= 50, f'Insufficient coverage: {len(board)}'
              assert (pd.to_datetime(board.history_through) < pd.Timestamp('2026-09-17')).all()
              assert not board.duplicated(['batter', 'game_pk']).any()
              board.head(50).to_csv(f'reports/sep17_{version}_top50.csv', index=False)
              print('History through:', board.history_through.unique().tolist())
              print(version, len(board), 'players;', board.expected_lineup.value_counts().to_dict())
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: sep17-preliminary-boards-and-model
          path: |
            reports/sep17*
            models/sep17*
          if-no-files-found: error
      - name: Save dated preview
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/sep17*
          git commit -m "Save September 17 preliminary V1 boards [skip ci]"
          git push
