name: september-14-preview
on:
  push:
    branches: [fix/pregame-data-integrity]
    paths: [.github/workflows/sep14-preview.yml]
permissions:
  contents: write
jobs:
  board:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -e '.[dev]'
      - run: python -m pytest -q
      - name: Update prior-day history
        run: daily-hr-ingest 2026-06-01 2026-09-13 data/raw/sep14_history.csv --chunk-days 7
      - name: Retrain corrected model
        run: |
          daily-hr-build data/raw/sep14_history.csv data/processed/sep14_training.parquet
          daily-hr-train data/processed/sep14_training.parquet --model-out models/sep14_pregame.joblib
      - name: Build preliminary boards
        run: |
          python -m daily_hr.daily_board data/raw/sep14_history.csv models/sep14_pregame.joblib models/sep14_pregame.features.json reports/sep14_inputs.csv --date 2026-09-14 --allow-projected
          python -m daily_hr.production_scorer reports/sep14_inputs.csv reports/sep14_v1_board.csv
          python - <<'PY'
          import pandas as pd
          for version in ('v1',):
              board = pd.read_csv(f'reports/sep14_{version}_board.csv')
              assert len(board) >= 50, f'Insufficient coverage: {len(board)}'
              assert (pd.to_datetime(board.history_through) < pd.Timestamp('2026-09-14')).all()
              assert not board.duplicated(['batter', 'game_pk']).any()
              board.head(50).to_csv(f'reports/sep14_{version}_top50.csv', index=False)
              print('History through:', board.history_through.unique().tolist())
              print(version, len(board), 'players;', board.expected_lineup.value_counts().to_dict())
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: sep14-preliminary-boards-and-model
          path: |
            reports/sep14*
            models/sep14*
          if-no-files-found: error
      - name: Save dated preview
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/sep14*
          git commit -m "Save September 14 preliminary V1 boards [skip ci]"
          git push
